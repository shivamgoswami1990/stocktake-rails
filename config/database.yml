default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  database: jkstocktake
  username: postgres
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  password:

production:
  <<: *default
  password:
